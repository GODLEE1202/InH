package COM.ex0420.am;

public class Ex03_Annotation {

	public static void main(String[] args) {
	/*
	 * Annotation
	 * 원래 기능은 알림을 위해 존재했던 기능
	 * 1.7에서 annotation을 통한 기능의 생성이 가능해지면서 완전히 다른 기능이 되버린다.
	 * elements : Annotation에 값을 저장할 수 있는 공간
	 * 				일반적인 변수라고 생각해도 무관하다.
	 * 				elements 선언시 어노테이션을 생성하고 반드시 해당 elements를 뒤의 괄호 안에 선언하여야 한다.
	 * 				elements에 default값을 놓을 경우 뒤에 default라는 명령을 두어 값을 선언할 수 있다.
	 * 				elements가 하나밖에 없거나 모든 elements가 default값을 갖고 있을 경우 value라는 이름을 주어  elements하나만 선언할 경우 이름을 생략 할수 있다.
	 * target : 어노테이션이 적용될 위치를 지정하는 옵션
	 * 			옵션은 총 10가지가 존재하며 대표적으로 클래스 멤버변수 메서드 생성자 등이 있다.
	 * retention : 어노테이션이 생존할수 있는 범위를 나타내며 source는 소스코드에서만, class는 컴파일된 클래스파일까지, runtime은 메모리에 코드가 로드될때까지 생존하도록 도와준다.
	 *  	
	 */

	}
	
}
